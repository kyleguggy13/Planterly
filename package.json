{
  "name": "planterly",
  "version": "0.1.0",
  "private": true,
  "description": "Planterly web frontend on Cloudflare Workers, backed by Firebase.",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "dev:worker": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:check": "wrangler deploy --dry-run",
    "test": "npm run build && node --test test/*.test.mjs && npm --prefix functions test",
    "lint": "node --check scripts/build.mjs && node --check scripts/dev.mjs && node --check worker/index.mjs && node --check sw.js && npm --prefix functions run lint"
  },
  "devDependencies": {
    "wrangler": "4.139.0"
  }
}
