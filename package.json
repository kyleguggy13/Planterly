{
  "name": "planterly",
  "version": "0.1.0",
  "private": true,
  "description": "Planterly web frontend, published with Sites and backed by Firebase.",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test test/build.test.mjs && npm --prefix functions test",
    "lint": "node --check scripts/build.mjs && node --check scripts/dev.mjs && node --check sw.js && npm --prefix functions run lint"
  }
}
